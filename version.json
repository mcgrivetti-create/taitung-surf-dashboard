{
  "assets": "2026-09-17c",
  "note": "Bump this together with ASSET_VERSION in js/app.js and the ?v= query in index.html. Fetched with cache:no-store, so it is always fresh even when index.html is not — that is what lets the page detect it is running stale code."
}
